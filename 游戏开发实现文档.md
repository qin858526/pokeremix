# 宝可梦：重组 — 游戏开发实现文档

> 基于 **Svelte 5 + TypeScript + Vite** 构建
> 战斗动画参考 [PokéRogue](https://github.com/pagefaultgames/pokerogue) 的 2D 精灵图 + CSS 动画实现
> 玩法参考：《宝可梦肉鸽》闯关框架 + 《心金魂银》对战工厂选队/交换机制

---

## 目录

1. [项目概述](#1-项目概述)
2. [技术架构总览](#2-技术架构总览)
3. [数据模型设计](#3-数据模型设计)
4. [核心游戏循环](#4-核心游戏循环)
5. [开局选队系统](#5-开局选队系统)
6. [战斗系统](#6-战斗系统)
7. [随机重组引擎](#7-随机重组引擎)
8. [战后交换系统](#8-战后交换系统)
9. [改造奖励系统](#9-改造奖励系统)
10. [关卡与敌人设计](#10-关卡与敌人设计)
11. [Boss 设计](#11-boss-设计)
12. [UI/UX 设计](#12-uiux-设计)
13. [状态管理](#13-状态管理)
14. [数据持久化](#14-数据持久化)
15. [与 PokeAPI 的集成](#15-与-pokeapi-的集成)
16. [开发路线图](#16-开发路线图)

---

## 1. 项目概述

### 1.1 游戏定位

《宝可梦：重组》是一款基于 Web 的宝可梦同人策略游戏。核心乐趣在于 **"在混乱中组建最优队伍"** —— 玩家从初始就被迫面对被打乱能力组合的宝可梦，并在战斗胜利后通过"交换"与"改造"不断优化队伍，体验策略博弈与天马行空组合的双重快感。

### 1.2 核心设计理念

| 原则 | 说明 |
|------|------|
| **混乱制造乐趣** | 每局游戏起点不同，属性/特性/技能完全随机重组 |
| **先做减法** | 先实现核心循环，再迭代其他系统 |
| **种族值为锚** | 种族值永远不变，保证宝可梦"本体强度"仍然重要 |
| **决策驱动** | 战斗后的交换与改造是核心策略节点，而非战斗本身 |

### 1.3 核心循环

```
开局选队 → [战斗 → 交换(必选) → 改造(三选一)]ⁿ → Boss战 → 击败冠军
```

---

## 2. 技术架构总览

### 2.1 技术栈

| 层级 | 技术 | 用途 |
|------|------|------|
| 框架 | Svelte 5 + Vite | 组件化 UI 开发（$state / $derived runes）|
| 语言 | TypeScript (严格模式) | 类型安全 |
| 渲染 | CSS 动画 + 2D 精灵图 | 战斗动画（暂不引入 3D）|
| 状态管理 | Svelte Stores + runes | 游戏状态管理 |
| 路由 | 基于 GamePhase 的条件渲染 | 单页无需路由库 |
| HTTP | fetch + API 抽象层 | 从 PokeAPI 获取数据 |
| 持久化 | localStorage + IndexedDB | 存档/设置 |
| 样式 | CSS 自定义属性 + 深色主题 | 主题/UI 风格 |

### 2.2 目录结构

```
src/
├── main.ts                  # 入口
├── App.svelte               # 根组件 + 路由
│
├── routes/                  # 页面级组件
│   ├── Home.svelte          # 标题画面
│   ├── TeamSelect.svelte    # 开局选队
│   ├── Battle.svelte        # 战斗场景（含 Boss 战）
│   ├── Reward.svelte        # 战后奖励(交换+改造)
│   ├── Victory.svelte       # 通关画面
│   └── GameOver.svelte      # 失败画面
│
├── components/              # 可复用组件
│   ├── PokemonCard.svelte   # 宝可梦信息卡片
│   ├── PokemonModel.svelte  # 3D模型展示
│   ├── HealthBar.svelte     # 血条组件
│   ├── BattleMenu.svelte    # 战斗指令菜单
│   ├── TypeBadge.svelte     # 属性标签
│   ├── ExchangePanel.svelte # 交换面板
│   ├── ModifyPanel.svelte   # 改造面板
│   ├── FloorIndicator.svelte# 关卡指示器
│   └── Transition.svelte    # 场景过渡动画
│
├── game/                    # 游戏逻辑(纯 TS)
│   ├── engine/
│   │   ├── BattleEngine.ts  # 战斗引擎
│   │   ├── DamageCalc.ts    # 伤害计算
│   │   ├── TypeChart.ts     # 属性克制表
│   │   └── AIController.ts  # 敌方 AI
│   ├── factory/
│   │   ├── RecombineFactory.ts  # 重组生成器
│   │   ├── TeamGenerator.ts     # 敌方队伍生成
│   │   └── ModifyGenerator.ts   # 改造选项生成
│   ├── state/
│   │   ├── GameState.ts     # 全局游戏状态
│   │   ├── BattleState.ts   # 战斗状态
│   │   └── RunState.ts      # 单局流程状态
│   └── data/
│       ├── PokemonDB.ts     # 宝可梦数据库(内存)
│       ├── MoveDB.ts        # 技能数据库
│       └── AbilityDB.ts     # 特性数据库
│
├── api/
│   ├── PokeAPIClient.ts     # PokeAPI HTTP 客户端
│   └── DataCache.ts         # 本地缓存层
│
├── stores/                  # Svelte Stores
│   ├── gameStore.ts         # 全局游戏 store
│   ├── battleStore.ts       # 战斗 store
│   └── uiStore.ts           # UI 状态 store
│
├── utils/
│   ├── random.ts            # 随机工具(种子随机)
│   └── helpers.ts           # 通用工具函数
│
└── styles/
    ├── global.css           # 全局样式
    ├── variables.css        # CSS 自定义属性
    └── animations.css       # 动画定义
```

### 2.3 数据流架构

```
PokeAPI
   │
   ▼
DataCache (IndexedDB) ───→ PokemonDB / MoveDB / AbilityDB (内存)
                                   │
                                   ▼
                          RecombineFactory (重组生成)
                                   │
                                   ▼
                          GameState Store ──→ 各页面组件
                                   │
                          ┌───────┼───────┐
                          ▼       ▼       ▼
                      BattleState  RunState  UI State
```

---

## 3. 数据模型设计

### 3.1 核心类型定义

#### 3.1.1 宝可梦实例（运行时）

```typescript
interface RecombinedPokemon {
  /** 唯一标识（运行时生成） */
  id: string;
  /** 全国图鉴编号，决定种族值底座 */
  dexId: number;
  /** 宝可梦名称 */
  name: string;

  // === 种族值（从 PokeAPI 获取，不可变更） ===
  baseStats: {
    hp: number;
    attack: number;
    defense: number;
    spAttack: number;
    spDefense: number;
    speed: number;
  };

  // === 个体值与努力值（固定值，不参与随机） ===
  ivs: Stats;        // 固定为全 31
  evs: Stats;        // 固定为全 0（暂不启用努力值系统）

  // === 重组部分（可被随机打乱和改造） ===
  types: [Type, Type | null];       // 属性（1-2个，可被改造替换）
  ability: Ability;                  // 特性（可被改造替换）
  moves: [Move, Move, Move, Move];  // 技能（最多4个，可被改造替换）

  // === 战斗状态 ===
  currentHp: number;
  maxHp: number;
  status: StatusCondition | null;
  statStages: StatStages;           // 能力变化阶段
}

interface Stats {
  hp: number;
  attack: number;
  defense: number;
  spAttack: number;
  spDefense: number;
  speed: number;
}
```

#### 3.1.2 类型枚举

```typescript
enum Type {
  Normal = 'normal',
  Fire = 'fire',
  Water = 'water',
  Electric = 'electric',
  Grass = 'grass',
  Ice = 'ice',
  Fighting = 'fighting',
  Poison = 'poison',
  Ground = 'ground',
  Flying = 'flying',
  Psychic = 'psychic',
  Bug = 'bug',
  Rock = 'rock',
  Ghost = 'ghost',
  Dragon = 'dragon',
  Dark = 'dark',
  Steel = 'steel',
  Fairy = 'fairy',
}
```

#### 3.1.3 技能

```typescript
interface Move {
  id: number;
  name: string;
  type: Type;
  category: 'physical' | 'special' | 'status';
  power: number;
  accuracy: number;
  pp: number;
  currentPp: number;
  priority: number;
  description: string;
  // 技能附加效果（如 10% 灼伤）
  effect?: MoveEffect;
}
```

#### 3.1.4 特性

```typescript
interface Ability {
  id: number;
  name: string;
  description: string;
  // 特性触发时机与效果描述，由 BattleEngine 解析
  trigger: AbilityTrigger;
}
```

### 3.2 战斗状态

```typescript
interface BattleState {
  phase: BattlePhase;
  turnCount: number;
  playerTeam: [RecombinedPokemon, RecombinedPokemon, RecombinedPokemon];
  enemyTeam: [RecombinedPokemon, RecombinedPokemon, RecombinedPokemon];
  playerActive: RecombinedPokemon;
  enemyActive: RecombinedPokemon;
  faintedOrder: string[];        // 倒下顺序（用于战后交换展示）
  weather: Weather | null;
  fieldEffects: FieldEffect[];
  revealedInfo: RevealedInfo;     // 敌方信息揭示状态（逐步揭示机制）
  battleLog: BattleLogEntry[];
}

type BattlePhase =
  | 'choose_action'
  | 'animating'
  | 'fainted_check'
  | 'victory'
  | 'defeat';

interface BattleLogEntry {
  turn: number;
  message: string;
  type: 'damage' | 'status' | 'heal' | 'fail' | 'effect';
}

/** 敌方信息揭示状态追踪 */
interface RevealedInfo {
  /** 特性是否已被触发揭示 */
  abilityRevealed: boolean;
  /** 已揭示的属性（初始为空数组，逐步添加） */
  revealedTypes: Type[];
  /** 已见过的技能列表（初始为空，敌方使用后加入） */
  seenMoves: SeenMove[];
}

interface SeenMove {
  moveId: number;
  /** 首次使用的回合 */
  turnRevealed: number;
}

/** 敌方宝可梦的外部展示状态（由 UI 层计算） */
interface EnemyDisplayState {
  name: string;
  types: (Type | '???')[];          // 未揭示的属性显示 ???
  ability: string | '???';
  knownMoves: KnownMove[];
  currentHp: number;
  maxHp: number;
  level: number;
}
```

### 3.3 单局流程状态

```typescript
interface RunState {
  floor: number;                         // 当前关卡数（从 1 开始）
  maxFloors: number;                     // 总关卡数
  bossFloors: number[];                  // Boss 关卡（如 [5, 10, 15, 20]）
  team: RecombinedPokemon[];             // 当前队伍（3只）
  faintedThisBattle: string[];           // 本场战斗倒下的己方宝可梦
  swapHistory: SwapRecord[];             // 交换历史
  modifyHistory: ModifyRecord[];         // 改造历史
  seed: number;                          // 本局种子
}

interface SwapRecord {
  floor: number;
  given: string;      // 交换出去的宝可梦 id
  received: string;   // 获得的宝可梦 id
}

interface ModifyRecord {
  floor: number;
  targetId: string;
  type: 'ability' | 'move' | 'type';
  oldValue: string;
  newValue: string;
}
```

---

## 4. 核心游戏循环

### 4.1 流程状态机

```
                ┌──────────────┐
                │   标题画面    │
                └──────┬───────┘
                       │ 开始新游戏
                       ▼
                ┌──────────────┐
                │  开局选队页   │ ← 从6只重组宝可梦中选3只
                └──────┬───────┘
                       │ 确认队伍
                       ▼
                ┌──────────────┐
          ┌───→ │  关卡层(floor)  │
          │     └──────┬───────┘
          │            │
          │     ┌──────▼───────┐
          │     │ 判断是否为Boss │ ← 根据 currentFloor
          │     └──┬───────┬───┘
          │    Yes │       │ No
          │        ▼       ▼
          │  ┌────────┐ ┌────────┐
          │  │ Boss战  │ │ 普通战  │
          │  └───┬────┘ └───┬────┘
          │      │          │ 胜利
          │      ▼          ▼
          │  ┌──────────────────────┐
          │  │   奖励流程（二阶段）   │
          │  │  1. 交换（必选）      │
          │  │  2. 改造（三选一）     │
          │  └──────────┬───────────┘
          │             │
          │     ┌───────▼───────┐
          │     │ 是否最后一关？  │
          │     └───┬───────┬───┘
          │     Yes │       │ No ──────┘
          │         ▼
          │  ┌──────────────┐
          │  │  通关/胜利    │
          │  └──────────────┘
          │
          └──── 失败 → Game Over ────→ 回到标题
```

### 4.2 关卡结构

```
总关卡数: 20 层 (常规 15 层 + Boss 5 层)
Boss 层: 5, 10, 15, 20 (第 20 层为冠军)

关卡难度曲线:
  层 1-4  → 简单 (敌人种族值总和 < 400)
  层 5    → Boss (馆主, 种族值 ~450)
  层 6-9  → 中等 (敌人种族值总和 400-500)
  层 10   → Boss (馆主, 种族值 ~500)
  层 11-14→ 困难 (敌人种族值总和 450-550)
  层 15   → Boss (馆主, 种族值 ~550)
  层 16-19→ 极难 (敌人种族值总和 500-600)
  层 20   → 冠军 (种族值总和 ~600+)
```

---

## 5. 开局选队系统

### 5.1 设计核心

参考《心金魂银》对战工厂的开局选队方式：
- 屏幕上显示 **6 个精灵球**
- 光标在球之间移动时，**只有当前选中的球才会展示其宝可梦的详细信息**
- 未选中的球保持"未知"的神秘感，只显示一个剪影或编号
- 这种设计增强了"盲选"的策略感和发现惊喜的乐趣

### 5.2 流程

```
1. 系统从全图鉴中随机抽取 6 只宝可梦（确保不重复）
2. 对每只宝可梦执行「随机重组」:
   - 保留种族值（底座）
   - 属性   → 随机从 18 种中抽取 1-2 个（保留原属性的概率 30%）
   - 特性   → 随机从全特性池中抽取（保留原特性的概率 20%）
   - 技能   → 随机从全技能池中抽取 4 个（至少 2 个攻击技能）
3. 6 个精灵球并排展示在屏幕上
4. 玩家通过方向键/鼠标悬浮移动光标
5. 被选中的球"打开"，展示对应宝可梦的详细信息
6. 玩家选择 3 只，确认后进入第 1 层
```

### 5.3 组件 `TeamSelect.svelte` — UI 布局

```
┌──────────────────────────────────────────────┐
│         选 择 你 的 初 始 队 伍               │
│                                              │
│  ┌──────┐  ┌──────┐  ┌──────┐  ┌──────┐   │
│  │  ●   │  │  ●   │  │ ▶●◀ │  │  ●   │   │
│  │ 球1  │  │ 球2  │  │ 球3  │  │ 球4  │   │
│  │      │  │      │  │选中  │  │      │   │
│  └──────┘  └──────┘  └──────┘  └──────┘   │
│  ┌──────┐  ┌──────┐                        │
│  │  ●   │  │  ●   │                        │
│  │ 球5  │  │ 球6  │                        │
│  │      │  │      │                        │
│  └──────┘  └──────┘                        │
│                                              │
│  ┌────────────────────────────────┐          │
│  │    球 3 — 详细情报（展开区域）   │          │
│  │                                │          │
│  │  [3D 模型 / 静态图]            │          │
│  │  宝可梦名称: 喷火龙             │          │
│  │  属性: [火] [飞行]              │          │
│  │  特性: 大力士                   │          │
│  │  技能: 喷射火焰 地震 龙爪 剑舞   │          │
│  │  种族值:                       │          │
│  │  HP ████████ 78               │          │
│  │  攻 ██████████ 84             │          │
│  │  防 ██████ 78                 │          │
│  │  特攻██████████ 109           │          │
│  │  特防██████ 85                │          │
│  │  速 ████████████ 100          │          │
│  └────────────────────────────────┘          │
│                                              │
│   已选择: 喷火龙 | ??? | ???                 │
│                                              │
│   ◀ ◀  ◀  光标(方向键/WASD)  ▶  ▶  ▶       │
│             [确认选择 (Enter)]               │
└──────────────────────────────────────────────┘
```

### 5.4 交互细节

| 操作 | 行为 |
|------|------|
| ← → 方向键 / A D | 在 6 个球之间水平移动光标 |
| ↑ ↓ 方向键 / W S | 在 6 个球之间垂直移动光标 |
| 鼠标悬停到球上 | 光标自动移动到该球 |
| 点击球 | 选中/取消选中该宝可梦（加入队伍） |
| Enter / 确认按钮 | 当已选 3 只时，确认出发 |
| 已选中宝可梦的球 | 球上显示选中标记（如小勾或发光边框）|

### 5.5 视觉状态

| 球的状态 | 视觉表现 |
|---------|---------|
| 未选中、未聚焦 | 闭合精灵球，仅显示编号（球1-6）|
| 光标聚焦 | 球微微放大 + 发光，底部展开详细信息面板 |
| 已选中 | 球上有 ✓ 标记，球变色（如变为红色）|
| 已选中且聚焦 | 球发光 + ✓ 标记，信息面板照常显示 |

---

## 6. 战斗系统

### 6.1 战斗流程

```
每回合:
  1. 速度判定 → 确定行动顺序
  2. 玩家选择行动（技能/换人）
  3. AI 选择行动
  4. 按速度顺序执行行动
  5. 伤害计算
  6. 状态判定（中毒/灼伤/睡眠等）
  7. 检查是否有宝可梦倒下
  8. 如一方全部倒下 → 战斗结束
  9. 否则进入下一回合
```

### 6.2 3v3 单打规则

- 双方各 3 只宝可梦，但场上始终只存在 1 只
- 一方宝可梦倒下后，下一只自动上场
- 换人消耗一回合行动（与主系列一致）
- 先攻方全部倒下 → 后攻方胜利

### 6.3 伤害计算公式

参照主系列第 5 代+ 的伤害公式：

```
伤害 = (((2 × 等级 / 5 + 2) × 威力 × (攻/防) / 50 + 2)
        × 修正 × 随机(0.85~1.0))

修正 = 属性一致 × 属性克制 × 会心 × 其他修正
```

**固定参数规定：**

| 参数 | 值 | 说明 |
|------|-----|------|
| 等级 | 50 | 全场固定，消除等级差距 |
| 个体值(IV) | 全 31 | 所有宝可梦一致，纯靠种族值和重组组合博弈 |
| 努力值(EV) | 全 0 | 暂不启用努力值系统，后续如需增加再做补充 |

由于等级、个体值、努力值全部固定，影响宝可梦实际能力的变量只剩下：
- **种族值**（不可变的底座）
- **重组组合**（属性 + 特性 + 技能的策略协同）

### 6.4 战斗引擎接口

```typescript
class BattleEngine {
  constructor(
    playerTeam: [RecombinedPokemon, RecombinedPokemon, RecombinedPokemon],
    enemyTeam: [RecombinedPokemon, RecombinedPokemon, RecombinedPokemon],
  );

  /** 玩家选择行动 */
  playerAction(action: PlayerAction): void;

  /** AI 自动选择行动 */
  enemyAction(): EnemyAction;

  /** 执行一回合，返回回合事件列表 */
  executeTurn(): TurnEvent[];

  /** 检查战斗结果 */
  checkBattleEnd(): 'player_win' | 'enemy_win' | null;

  /** 获取当前战斗快照 */
  getSnapshot(): BattleSnapshot;
}
```

### 6.5 战斗 UI 布局（参考心金魂银宝可梦视角）

采用《心金魂银》的**第一人称宝可梦视角**：我方宝可梦位于画面**下方**（从训练师角度看出去），敌方宝可梦位于画面上方。双方信息窗口在对应位置叠加显示。

```
┌──────────────────────────────────────────┐
│  关卡: 3/20                              │
│                                          │
│  ┌─────────────────────────┐             │
│  │                         │             │
│  │    敌方宝可梦 (3D/2D)    │  LV.50      │
│  │     [属性: ???]         │  ┌────────┐ │
│  │                         │  │ HP 142 │ │
│  │                         │  │ ██████ │ │
│  └─────────────────────────┘  └────────┘ │
│                                          │
│                                          │
│         ~~~ 对战场地 ~~~                 │
│                                          │
│  ┌─────────────────────────┐             │
│  │                         │  ┌────────┐ │
│  │    我方宝可梦 (3D/2D)    │  │ HP 98  │ │
│  │     [属性: ???]         │  │ █████  │ │
│  │                         │  └────────┘ │
│  └─────────────────────────┘             │
│                                          │
│  ┌──────┬──────┬──────┬──────┐           │
│  │ 技能1 │ 技能2 │ 技能3 │ 技能4 │           │
│  │  火   │  地   │  龙   │  普   │           │
│  │ PP8   │ PP10  │ PP5   │ PP20  │           │
│  ├──────┴──────┴──────┴──────┤           │
│  │         [换人]             │           │
│  └───────────────────────────┘           │
└──────────────────────────────────────────┘
```

#### 视角说明

- **我方视角**：画面从训练师位置看向对战场地，我方宝可梦呈现在画面**底部近端**（显示背部/侧影），敌方在**顶部远端**
- **信息窗口**：HP 条紧随对应宝可梦摆放，敌方 HP 在右上角，我方 HP 在左下角，符合玩家视线自然移动轨迹
- **技能菜单**：位于画面底部中央，4 个技能以 2×2 或 1×4 布局排列，显示属性图标和 PP 余量
- **敌方信息模糊**：敌方属性的详情遵循第 10.3 节的可见性规则，初次上场时不显示

### 6.6 特性系统与战斗流程的兼容设计

> 特性会在战斗流程的多个节点触发，必须在战斗引擎设计中**内置特性钩子系统**，而非事后追加。以下是完整的特性介入点。

#### 6.6.1 特性触发时机矩阵

```
特性在以下时机均可能触发（按战斗流程排序）:

┌─ 上场时 ─────────────────────────────────┐
│  威吓(对手攻击-1)     │ 压迫感(对手PP-1)    │
│  天气(扬沙/降雨等)    │ 静电/孢子(接触30%)  │
│  幻觉(复制对方外形)   │ 预知梦(显示对手技能)  │
│  换气(上场加速)       │ 察觉(显示对手道具)   │
└──────────────────────────────────────────┘

┌─ 技能命中前 ──────────────────────────────┐
│  漂浮(免疫地面)       │ 贮水/蓄电(免疫并回血)│
│  引火(免疫火并提升)   │ 干燥皮肤(火1.25×)   │
│  神奇守护(仅受克制)   │ 过滤/坚石(克制-25%) │
│  多重鳞片(满血减半)   │ 白烟(防能力下降)     │
└──────────────────────────────────────────┘

┌─ 伤害计算时 ──────────────────────────────┐
│  大力士/瑜伽力(攻×2)  │ 厚脂肪(冰/火½)      │
│  加速(每回合速度+1)   │ 斗争心(同性+25%)    │
│  钢能力上升(钢系+½)   │ 适应力(一致×2)      │
│  技术高手(威力≤60×1.5) │ 铁拳(拳击×1.2)     │
└──────────────────────────────────────────┘

┌─ 受到攻击后 ──────────────────────────────┐
│  坚硬脑袋(无反伤)     │ 咒术之躯(30%定身)   │
│  孢子(接触30%中毒等)  │ 同步(传递异常状态)   │
│  食草(免疫草并+攻)    │ 神奇鳞片(异常时防×1.5)│
└──────────────────────────────────────────┘

┌─ 回合结束时 ──────────────────────────────┐
│  剩饭/黑色淤泥(回血)  │ 毒疗(中毒时回血)    │
│  加速(再判定一次)      │ 降雨/日照(续天气)   │
│  铁刺/粗糙皮肤(接触伤) │ 压迫感(再判定)      │
└──────────────────────────────────────────┘

┌─ 替换/倒下时 ────────────────────────────┐
│  自然回复(下场解异常)  │ 蜕皮(回合末解异常)  │
│  迟钝(不被挑衅/着迷)   │ 精神力(不害怕)      │
│  引爆(近战倒下炸1/4)   │ 木乃伊(传递特性)    │
└──────────────────────────────────────────┘
```

#### 6.6.2 引擎中的特性钩子

```typescript
class BattleEngine {
  /** 特性钩子注册表 ── 每个时机对应一组可触发的特性处理器 */
  private abilityHooks: Map<AbilityTriggerTiming, AbilityHandler[]>;

  /**
   * 战斗引擎初始化时，为场上每只宝可梦的当前特性
   * 注册对应的钩子函数
   */
  constructor(playerTeam, enemyTeam) {
    this.abilityHooks = new Map();
    this.registerAbility(playerTeam[0]);
    this.registerAbility(enemyTeam[0]);
  }

  /** 为一只宝可梦的特性注册所有相关钩子 */
  private registerAbility(pokemon: RecombinedPokemon): void {
    const ability = pokemon.ability;
    const timings = ABILITY_TRIGGER_MAP[ability.id];
    for (const timing of timings) {
      this.abilityHooks.get(timing)?.push({
        source: pokemon,
        handler: ability.effect,
      });
    }
  }

  /** 执行指定时机的全部特性钩子 */
  private executeAbilityHooks(
    timing: AbilityTriggerTiming,
    context: AbilityContext,
  ): void {
    const hooks = this.abilityHooks.get(timing) ?? [];
    for (const hook of hooks) {
      if (hook.source.currentHp > 0) {
        hook.handler(context, hook.source);
      }
    }
  }

  /**
   * 完整的回合执行流程（含特性介入点）:
   */
  executeTurn(): TurnEvent[] {
    const events: TurnEvent[] = [];

    // ① 回合开始 → 特性钩子（如天气续时、加速）
    this.executeAbilityHooks('turn_start', this.getContext());

    // ② 速度判定 → 确定行动顺序
    this.calculateSpeedOrder();

    // ③ 先攻方行动
    const firstAction = this.actionQueue[0];
    // ③a 技能选择后 → 特性钩子（如静电预判定）
    this.executeAbilityHooks('before_move', this.getContext(firstAction));
    // ③b 技能命中性判定 → 特性钩子（如漂浮免疫地面）
    if (this.checkHit(firstAction)) {
      // ③c 伤害计算 → 特性钩子（大力士/厚脂肪等）
      this.executeAbilityHooks('damage_calc', this.getContext(firstAction));
      const damage = this.calculateDamage(firstAction);
      this.applyDamage(firstAction.target, damage);
      // ③d 受到攻击后 → 特性钩子（咒术之��/孢子等）
      this.executeAbilityHooks('after_hit', this.getContext(firstAction));
    }

    // ④ 后攻方行动（同上结构）
    // ...

    // ⑤ 回合结束时 → 特性钩子（剩饭/毒疗等）
    this.executeAbilityHooks('turn_end', this.getContext());

    // ⑥ 倒下检查
    this.checkFaint();

    return events;
  }
}
```

#### 6.6.3 特性触发条件定义

```typescript
interface AbilityTrigger {
  id: string;
  /** 触发时机 */
  timing: AbilityTriggerTiming;
  /** 判定条件函数 */
  condition?: (context: AbilityContext) => boolean;
  /** 效果执行函数 */
  execute: (context: AbilityContext, source: RecombinedPokemon) => void;
}

type AbilityTriggerTiming =
  | 'on_switch_in'       // 上场时
  | 'before_move'        // 技能命中前
  | 'damage_calc'        // 伤害计算时
  | 'after_hit'          // 受到攻击后
  | 'turn_start'         // 回合开始时
  | 'turn_end'           // 回合结束时
  | 'on_faint'           // 倒下时
  | 'on_switch_out';     // 替换下场时

interface AbilityContext {
  attacker: RecombinedPokemon;
  defender: RecombinedPokemon;
  move: Move | null;
  weather: Weather | null;
  fieldEffects: FieldEffect[];
  damage: number;
  battleLog: BattleLogEntry[];
}
```

#### 6.6.4 实现策略

| 优先级 | 特性类型 | 实现方式 | 示例 |
|--------|---------|---------|------|
| P0 | 伤害修正类 | 在 DamageCalc 中插入系数修正 | 大力士(×2)、厚脂肪(×½) |
| P0 | 免疫类 | 在命中判定前检查 | 漂浮(免疫地面)、引火(免疫火) |
| P0 | 能力变化类 | 在 StatStage 系统中处理 | 威吓(攻击-1)、加速(速度+1) |
| P1 | 回合末恢复类 | 在 turn_end 钩子执行 | 剩饭、毒疗 |
| P1 | 接触效果类 | 在 after_hit 钩子判定 | 静电(30%)、孢子(30%) |
| P2 | 信息获取类 | 触发后在 UI 层暴露信息 | 预知梦、察觉 |
| P2 | 天气类 | 随天气系统实现 | 扬沙、降雨、干旱 |

---

## 7. 随机重组引擎

### 7.1 重组算法

```typescript
class RecombineFactory {
  /**
   * 对一只宝可梦执行完全随机重组
   * 种族值 → 不变
   * 属性   → 从全 Type 中随机选 1-2 个
   * 特性   → 从全 Ability 池中随机抽取 1 个
   * 技能   → 从全 Move 池中随机抽取 4 个（至少 2 个攻击技能）
   */
  static fullyRecombine(base: PokemonSpecies): RecombinedPokemon;

  /**
   * 部分随机重组（用于Boss战"最后一只是随机组合"的场景）
   * 只对指定的部分执行随机，其他保持原样
   */
  static partiallyRecombine(
    base: PokemonSpecies,
    parts: ('types' | 'ability' | 'moves')[],
  ): RecombinedPokemon;

  /**
   * 保留原属性的概率控制
   * 属性保留原属性概率: 30%
   * 特性保留原特性概率: 20%
   */
  static withRetention(base: PokemonSpecies, retentionRates: RetentionRates): RecombinedPokemon;
}
```

### 7.2 种子随机

为保证同一局游戏的可复现性（调试/分享），使用种子随机数：

```typescript
// 使用 32 位整数作为种子
// 每局开始时从 Date.now() 或玩家输入生成种子
// 所有随机操作均通过种子随机数执行

class SeededRandom {
  constructor(seed: number);
  /** 返回 [0, max) 的整数 */
  nextInt(max: number): number;
  /** 从数组中随机选一个 */
  pick<T>(arr: T[]): T;
  /** 洗牌（Fisher-Yates） */
  shuffle<T>(arr: T[]): T[];
}
```

---

## 8. 战后交换系统

### 8.1 逻辑

```typescript
interface ExchangeSystem {
  /**
   * 获取可选的交换目标列表
   * 从敌方 3 只宝可梦中选取未倒下的
   * 若敌人全部倒下，可从其全部 3 只中任选
   */
  getExchangeOptions(
    battleResult: BattleResult,
    enemyTeam: RecombinedPokemon[],
  ): ExchangeOption[];

  /**
   * 执行交换
   * 玩家从己方队伍选 1 只 → 与选中的敌方宝可梦交换
   * 返回新的玩家队伍
   */
  executeExchange(
    playerTeam: RecombinedPokemon[],
    enemyPokemon: RecombinedPokemon,
    playerPokemonIndex: number,
  ): RecombinedPokemon[];

  /**
   * 验证交换合法性
   * - 不能交换不存在的宝可梦
   * - 交换后队伍总数为 3
   */
  validateExchange(
    playerTeam: RecombinedPokemon[],
    enemyPokemon: RecombinedPokemon,
    playerPokemonIndex: number,
  ): boolean;
}
```

### 8.2 交换 UI

```
┌─────────────────────────────────────────────┐
│            战 后 交 换（必选）                │
│                                             │
│  从对方队伍中选择 1 只宝可梦进行交换：          │
│                                             │
│  ┌──────┐    ┌──────┐    ┌──────┐          │
│  │敌方1  │    │敌方2  │    │敌方3  │          │
│  │属性   │    │属性   │    │属性   │          │
│  │特性   │    │特性   │    │特性   │          │
│  │技能   │    │技能   │    │技能   │          │
│  │种族   │    │种族   │    │种族   │          │
│  └──────┘    └──────┘    └──────┘          │
│                                             │
│  从己方队伍中选择要换出的宝可梦：              │
│  ┌──────┐    ┌──────┐    ┌──────┐          │
│  │我方1  │    │我方2  │    │我方3  │          │
│  │  ✓   │    │      │    │      │          │
│  └──────┘    └──────┘    └──────┘          │
│                                             │
│  [确认交换]  [查看详细信息]                    │
└─────────────────────────────────────────────┘
```

---

## 9. 改造奖励系统

### 9.1 流程

```
1. 交换完成后进入改造界面
2. 系统从以下 3 类中各抽 1 个选项：
   - 特性改造：随机 1 个新特性
   - 技能改造：随机 1 个新技能
   - 属性改造：随机 1 个新属性
3. 玩家三选一
4. 玩家选择队伍中任意 1 只宝可梦作为改造目标
5. 应用改造
```

### 9.2 选项生成

```typescript
class ModifyGenerator {
  /**
   * 生成 3 个改造选项（每类一个）
   */
  static generateOptions(): ModifyOption[];

  /**
   * 特性改造
   * - 不会生成目标已拥有的特性
   * - 从全特性池随机抽取（排除目标当前特性）
   */
  static generateAbilityOption(target: RecombinedPokemon): ModifyOption;

  /**
   * 技能改造
   * - 不会生成目标已拥有的技能
   * - 从全技能池随机抽取（排除目标当前4个技能）
   * - 优先选取与目标属性同属性的技能（概率+50%）
   */
  static generateMoveOption(target: RecombinedPokemon): ModifyOption;

  /**
   * 属性改造
   * - 随机替换目标的第 1 属性或第 2 属性
   * - 若目标只有一个属性，则添加为第 2 属性（不会降为0属性）
   * - 不会生成目标已有的属性
   */
  static generateTypeOption(target: RecombinedPokemon): ModifyOption;
}
```

### 9.3 改造 UI

```
┌───────────────────────────────────────────┐
│           打 造 改 造（三选一）              │
│                                           │
│  选择一个改造项目：                          │
│                                           │
│  ┌─────────────┐                          │
│  │  特性改造    │  ◄ 选中                     │
│  │  新特性：大力士│                          │
│  │  效果：攻击×2 │                          │
│  └─────────────┘                          │
│                                           │
│  ┌─────────────┐                          │
│  │  技能改造    │                           │
│  │  新技能：地震 │                           │
│  │  威力：100   │                           │
│  └─────────────┘                          │
│                                           │
│  ┌─────────────┐                          │
│  │  属性改造    │                           │
│  │  新属性：龙  │                           │
│  └─────────────┘                          │
│                                           │
│  选择改造目标：                              │
│  [宝可梦1] [宝可梦2] [宝可梦3]                │
│                                           │
│  [确认改造]                                 │
└───────────────────────────────────────────┘
```

---

## 10. 关卡与敌人设计

### 10.1 敌人阵容生成策略

#### 核心思路：预设组合 + 随机抽取

每一只 NPC 宝可梦不再由纯随机重组生成，而是从**预设组合库**中随机抽取一套组合。

预设组合参考**对战热门数据**（Pokemon Showdown / Smogon 热门配置），确保每个组合在实战中是合理的：
- 属性与特性有协同性
- 技能池有战术逻辑（如"物攻手带剑舞 + 本系技能 + 补盲技能"）
- 输出/防守/辅助分工明确

这样既保留了"地雷配置"的惊喜感，又避免了"完全随机导致乱出毫无逻辑的技能"的糟糕体验。

#### 预设组合数据结构

```typescript
interface PresetCombo {
  /** 组合名称（用于调试/展示） */
  label: string;
  /** 属性组合，空数组表示不覆盖重组引擎的随机 */
  types?: [Type, Type | null];
  /** 特性 */
  ability: number;       // 特性 ID
  /** 技能（4个） */
  moves: number[];       // 技能 ID 数组
  /** 战术标签，用于组合逻辑 */
  tags: ('physical' | 'special' | 'support' | 'setup' | 'stall' | 'hazard' | 'weather')[];
}

interface PresetComboLibrary {
  /** 宝可梦 ID → 该宝可梦可用的预设组合列表 */
  [dexId: number]: PresetCombo[];
}
```

#### 预设组合示例

```typescript
// 针对宝可梦 #6 喷火龙 的几套预设组合
const CHARIZARD_COMBOS: PresetCombo[] = [
  {
    label: '特攻炮台',
    types: ['Fire', 'Flying'],
    ability: 67,  // 太阳之力
    moves: [52, 53, 89, 157], // 喷射火焰, 气象球, 大地之力, 过热
    tags: ['special'],
  },
  {
    label: '腹鼓物攻',
    types: ['Fire', 'Dark'],
    ability: 66,  // 猛火
    moves: [203, 404, 89, 444], // 腹鼓, 闪焰冲锋, 大地之力, 龙爪
    tags: ['physical', 'setup'],
  },
  {
    label: '晴天辅助',
    types: ['Fire', 'Grass'],
    ability: 213, // 日照
    moves: [207, 472, 412, 52], // 日光束, 过热, 龙之舞, 喷射火焰
    tags: ['special', 'weather'],
  },
];
```

#### 生成流程

```
1. 根据关卡和难度确定宝可梦池（种族值区间）
2. 从宝可梦池中随机抽取 3 只（不重复）
3. 对每只宝可梦：
   a. 从该宝可梦的预设组合库中随机抽取 1 套组合
   b. 种族值保持宝可梦本身的数值
   c. 属性/特性/技能按预设组合覆盖
4. 生成完整敌方队伍
```

#### 预设组合来源优先级

| 来源 | 优先级 | 说明 |
|------|--------|------|
| 手动编写对战热门配置 | 最高 | 参考 Smogon OU / Showdown 统计数据 |
| 从 PokeAPI 学习该宝可梦的"招牌技能" | 中 | 技能池中加入各代经典配置 |
| 完全随机重组（兜底） | 低 | 确保每种宝可梦即使无预设组合也有配置 |

初期开发时每只宝可梦先实现 2-3 套预设组合，后续可随数据积累持续扩充。

### 10.2 关卡难度曲线

```
总关卡数: 20 层 (常规 15 层 + Boss 5 层)
Boss 层: 5, 10, 15, 20 (第 20 层为冠军)

关卡难度曲线:
  层 1-4  → 简单 (敌人种族值总和 < 400)
  层 5    → Boss (馆主, 种族值 ~450)
  层 6-9  → 中等 (敌人种族值总和 400-500)
  层 10   → Boss (馆主, 种族值 ~500)
  层 11-14→ 困难 (敌人种族值总和 450-550)
  层 15   → Boss (馆主, 种族值 ~550)
  层 16-19→ 极难 (敌人种族值总和 500-600)
  层 20   → 冠军 (种族值总和 ~600+)
```

### 10.3 敌人队伍生成器

```typescript
class TeamGenerator {
  /**
   * 根据关卡和难度生成敌方队伍
   * @param floor 当前关卡
   * @param seed 本局种子
   * @param comboLibrary 预设组合库
   */
  static generateEnemyTeam(
    floor: number,
    seed: number,
    comboLibrary: PresetComboLibrary,
  ): RecombinedPokemon[];

  /**
   * 获取该关卡的随机宝可梦池
   * 低层：从 150-300 种族值区间抽取
   * 中层：从 300-480 种族值区间抽取
   * 高层：从 450-600 种族值区间抽取
   */
  static getPokemonPool(floor: number): PokemonSpecies[];

  /**
   * 为一只宝可梦抽取预设组合
   * 有预设组合 → 随机抽一套
   * 无预设组合 → 兜底使用随机重组
   */
  static pickCombo(
    species: PokemonSpecies,
    comboLibrary: PresetComboLibrary,
    rng: SeededRandom,
  ): PresetCombo;
}
```

### 10.4 敌方信息可见性规则

根据你的调整，敌方信息采用**逐步揭示**策略，保留"地雷配置"的乐趣。

#### 特性可见性

| 阶段 | 特性显示 | 说明 |
|------|---------|------|
| 战斗开始 | `???` | 完全隐藏 |
| 特性触发后 | 常驻显示特性名称和效果 | 触发一次即永久揭示，后续不再隐藏 |
| 战斗中 | 显示为`???[已触发]` → 触发后变为 `大力士` | 触发前后在战斗日志中有明确记录 |

#### 属性可见性

| 揭示条件 | 行为 |
|---------|------|
| 默认（上场时） | 不显示敌方属性，显示为`???` |
| 己方技能命中且效果绝佳(×2) | 触发提示"效果绝佳！"，并在敌方 HUD 上**揭示对应属性** |
| 己方技能命中且效果不好(×½) | 触发提示"效果不太好…"，并在敌方 HUD 上**揭示对应属性** |
| 己方技能完全无效(×0) | 触发提示"对 XXX 没有效果…"，**揭示敌方的免疫属性** |
| 敌方使用符合自身属性的技能 | 敌方出招时，在技能名称旁显示 STAB 属性标记 |
| 该属性已被揭示后 | 该属性常驻显示，不再隐藏 |

#### 实战示例

```
回合 1:
  我方使用"喷射火焰" → 命中 → 显示"效果绝佳！"
  揭示: 敌方拥有 [草] 属性 (敌方 HUD: [???, ???] → [草, ???])

回合 2:
  敌方使用"冲浪" → 水属性技能，与敌方属性匹配
  揭示: 敌方拥有 [水] 属性 (敌方 HUD: [草, ???] → [草, 水])

现在敌方的双属性已完全揭示，此后常驻显示 [草] [水]
```

#### 其他信息可见性总表

| 信息项 | 可见性 | 说明 |
|--------|--------|------|
| 宝可梦名称 | ✅ 始终可见 | 上场即显示 |
| HP 条 | ✅ 始终可见 | 实时更新 |
| 等级 | ✅ 始终可见 | Lv.50 固定显示 |
| 特性 | 🔒 触发后才显示 | 首次触发后常驻，否则显示 `???` |
| 属性 | 🔒 逐步揭示 | 效果绝佳/不好/免疫时揭示对应属性 |
| 技能 | 🔒 使用后才记录 | 敌方使用过的技能进入战斗日志 |
| 种族值 | ❌ 永不可见 | 玩家凭经验判断"这只看起来很肉" |

---

## 11. Boss 设计

### 11.1 Boss 结构

```
Boss 战发生在第 5、10、15、20 层

每个 Boss 有:
  - 名称（如"重组馆主·炎"）
  - 3 只宝可梦（全部完全重组）
  - 种族值在该层段处于中上水平
  - Boss 特殊 tag（用于展示和氛围）
  - 击败后触发特殊的胜利动画

第 20 层 Boss（冠军）:
  - 名称：重组联盟冠军
  - 宝可梦种族值 580+
  - 全部完全重组
  - 带有"冠军气场"特殊效果
```

### 11.2 Boss 预置数据

```typescript
interface BossTemplate {
  name: string;
  title: string;
  floor: number;
  pokemonSpecies: number[];     // 3 只宝可梦的图鉴编号
  minBST: number;               // 最小种族值总和
  recombineLevel: RecombineLevel;
  specialRule?: BossSpecialRule;
}

const BOSS_TEMPLATES: BossTemplate[] = [
  { name: '重组馆主·岩', title: '坚岩之道', floor: 5, pokemonSpecies: [], minBST: 450, recombineLevel: 'full' },
  { name: '重组馆主·钢', title: '钢铁之壁', floor: 10, pokemonSpecies: [], minBST: 500, recombineLevel: 'full' },
  { name: '重组馆主·龙', title: '龙之狂乱', floor: 15, pokemonSpecies: [], minBST: 530, recombineLevel: 'full' },
  { name: '重组冠军', title: '混沌之主', floor: 20, pokemonSpecies: [], minBST: 580, recombineLevel: 'full' },
];
```

---

## 12. UI/UX 设计

### 12.1 布局框架

```
┌──────────────────────────────────────────┐
│  顶部 HUD: 关卡数 | 时间 | 设置 | 退出    │
├──────────────────────────────────────────┤
│                                          │
│             主 内 容 区 域                │
│         (视当前页面而定)                   │
│                                          │
├──────────────────────────────────────────┤
│  底部: 对话/提示文字区域                    │
└──────────────────────────────────────────┘
```

### 12.2 设计规范

| 维度 | 规范 |
|------|------|
| 字号层级 | h1: 32px / h2: 24px / h3: 20px / body: 16px / small: 14px |
| 颜色主题 | 深色背景为主（参考宝可梦肉鸽风格） |
| 宝可梦属性色 | 18 种属性各对应一种主题色 |
| 间距单位 | 4px 为基础步进（4/8/12/16/20/24/32/48） |
| 圆角 | 卡片: 12px / 按钮: 8px / 标签: 4px |
| 过渡动画 | 统一 300ms ease 作为默认过渡时间 |
| 响应式 | 最小宽度 320px，建议 1024×768 以上体验最佳 |

### 12.3 关键交互细节

- **战斗指令**：点击技能卡 → 弹出确认 → 执行
- **宝可梦切换**：战斗中点"换人" → 可选列表弹出 → 选择
- **交换选队**：先点敌方 → 再点己方 → 确认交换（两步式防误触）
- **改造选项**：鼠标悬浮时在右侧显示"改造预览"效果
- **场景过渡**：战斗结束时画面淡出 → 交换界面滑入

### 12.4 CSS 动画 + 2D 精灵图

战斗动画采用 **CSS animation + 精灵图切换（<img>）** 实现，覆盖以下交互：

```typescript
interface BattleAnimation {
  /** 入场动画 */
  entryAnimation: 'fade_in' | 'slide_up' | 'ball_open';
  /** 攻击动画 */
  attackAnimation: 'lunge' | 'charge';
  /** 受伤动画 */
  hitAnimation: 'shake' | 'flash';
  /** 倒下动画 */
  faintAnimation: 'fade_out' | 'fall';
}
```

> **3D 模型说明**：文档原定使用 Three.js / Threlte 展示宝可梦 3D 模型，经评估后决定**暂不引入 3D**。原因：
> - 战斗动画用 CSS 动画 + 2D 精灵图即可实现等效效果
> - 参考 PokéRogue 同样使用 2D 精灵图，效果优秀
> - 减少项目复杂度，加快开发进度
> - 3D 模型可在后期按需加入，不影响现有架构

---

## 13. 状态管理

### 13.1 Svelte Store 结构

#### `gameStore.ts` — 全局游戏状态

```typescript
interface GameStore {
  /** 游戏阶段 */
  phase: 'title' | 'team_select' | 'battle' | 'reward_exchange' | 'reward_modify' | 'victory' | 'game_over';
  /** 当前关卡 */
  currentFloor: number;
  /** 玩家队伍 */
  playerTeam: RecombinedPokemon[];
  /** 本局种子 */
  seed: number;
  /** 是否首次完成（用于解锁内容） */
  firstClear: boolean;

  // Actions
  startNewRun: () => void;
  setTeam: (team: RecombinedPokemon[]) => void;
  advanceFloor: () => void;
  endRun: (won: boolean) => void;
}
```

#### `battleStore.ts` — 战斗状态

```typescript
interface BattleStore {
  enemyTeam: RecombinedPokemon[];
  playerActive: RecombinedPokemon | null;
  enemyActive: RecombinedPokemon | null;
  turnLog: BattleLogEntry[];
  isAnimating: boolean;

  // Actions
  initBattle: (enemy: RecombinedPokemon[]) => void;
  executeTurn: (action: PlayerAction) => Promise<void>;
  handleFaint: () => void;
  endBattle: () => BattleResult;
}
```

### 13.2 派生 Store

```typescript
// 当前出战宝可梦的状态
const activePokemon = derived(gameStore, $game => {
  return $game.playerTeam.find(p => !p.fainted) ?? null;
});

// 本层是否 Boss 层
const isBossFloor = derived(gameStore, $game => {
  return BOSS_FLOORS.includes($game.currentFloor);
});
```

---

## 14. 数据持久化

### 14.1 存档结构

```typescript
interface SaveData {
  version: number;                    // 存档版本号
  timestamp: number;                  // 保存时间
  runState: RunState;                 // 单局流程
  gamePhase: string;                  // 当前阶段
  playerTeam: SerializedPokemon[];    // 序列化的队伍数据
  lastBattleResult?: BattleResult;    // 上次战斗结果（断点续战用）
}
```

### 14.2 存储策略

| 数据 | 存储方式 | 说明 |
|------|---------|------|
| 进行中的游戏 | localStorage | 1 个槽位，JSON 序列化 |
| 设置 | localStorage | 音效/画质/种子偏好 |
| PokeAPI 缓存 | IndexedDB | 减少 API 请求 |
| 完成记录 | localStorage | 通关次数/统计 |

### 14.3 自动存档

```
触发时机:
  - 开局选队确认后
  - 每次战斗胜利后（进入交换前）
  - 每次改造完成后

每步仅保留一个存档点（覆盖），不保留历史。
```

---

## 15. 与 PokeAPI 的集成

### 15.1 数据获取计划

```typescript
class PokeAPIClient {
  private cache: DataCache;

  constructor() {
    this.cache = new DataCache();
  }

  /** 获取全部宝可梦基础数据 */
  async fetchAllPokemon(): Promise<PokemonSpecies[]>;

  /** 获取指定宝可梦的数据 */
  async fetchPokemon(dexId: number): Promise<PokemonSpecies>;

  /** 获取全部技能 */
  async fetchAllMoves(): Promise<Move[]>;

  /** 获取全部特性 */
  async fetchAllAbilities(): Promise<Ability[]>;
}
```

### 15.2 数据缓存层

```typescript
class DataCache {
  /**
   * 优先从 IndexedDB 读取，未命中则从 PokeAPI 获取
   * PokeAPI 数据永久缓存（不常变化）
   */
  async getOrFetch<T>(key: string, fetcher: () => Promise<T>): Promise<T>;

  /** 批量预加载 */
  async preloadAll(): Promise<void>;
}
```

### 15.3 首次加载策略

- 首次启动：从 PokeAPI 拉取全部宝可梦列表、技能列表、特性列表（总计 ~1300 只宝可梦，需分页请求）
- 显示加载进度条（预计 30-60 秒）
- 缓存到 IndexedDB 后，后续启动秒开
- **一旦缓存就绪，后续游戏无需联网**

### 15.4 所需 PokeAPI 端点

| 用途 | 端点 |
|------|------|
| 全宝可梦列表 | `/pokemon?limit=1300` |
| 宝可梦详情 | `/pokemon/{id}` |
| 种族值 | `/pokemon/{id}` → stats |
| 全技能列表 | `/move?limit=1000` |
| 技能详情 | `/move/{id}` |
| 全特性列表 | `/ability?limit=300` |
| 特性详情 | `/ability/{id}` |

---

## 16. 开发路线图

### 阶段 1：数据层 + 核心引擎 (2-3 周)

- [x] 初始化项目（Svelte 5 + TypeScript + Vite）
- [ ] 实现 PokeAPI 数据拉取 + IndexedDB 缓存
- [ ] 构建 PokemonDB / MoveDB / AbilityDB 内存数据库
- [ ] 实现种子随机数工具 SeededRandom
- [ ] 实现伤害计算引擎 DamageCalc + TypeChart
- [ ] 实现 RecombineFactory 重组生成器
- [ ] 单元测试覆盖核心引擎（Vitest）

### 阶段 2：核心游戏循环 (2-3 周)

- [ ] 实现 GameState / RunState 状态管理
- [ ] 实现开局选队页面 TeamSelect
- [ ] 实现基础战斗场景 Battle（含 3v3 规则）
- [ ] 实现敌方 AI 控制器
- [ ] 实现战斗状态与动画流程
- [ ] 实现 TeamGenerator 敌人队伍生成
- [ ] 集成分层难度策略

### 阶段 3：交换 + 改造系统 (1-2 周)

- [ ] 实现战后交换系统
- [ ] 实现改造选项生成 ModifyGenerator
- [ ] 实现改造界面 ExchangePanel + ModifyPanel
- [ ] 实现关卡流程控制（胜利 → 交换 → 改造 → 下一关）
- [ ] Boss 战特殊逻辑

### 阶段 4：UI 打磨 + 3D 模型 (2-3 周)

- [ ] 集成 Three.js 宝可梦 3D 模型展示
- [ ] 战斗动画（技能特效/切换/倒下/胜利）
- [ ] 场景过渡动画（回合间/关卡间）
- [ ] UI 主题色完善（属性色/状态色）
- [ ] 响应式布局适配
- [ ] 音效与背景音乐（预留接口）

### 阶段 5：额外系统 + 完善 (2 周)

- [ ] 存档/读档功能
- [ ] 设置页面（音效/画质/控制）
- [ ] 游戏统计（通关次数/胜率）
- [ ] 通关后解锁内容（如种子码分享）
- [ ] 错误降级 UI（PokeAPI 下载失败等）
- [ ] 加载状态与骨架屏

### 总计：约 8-12 周（视投入精力调整）

---

## 附录

### A. 技术风险与缓解

| 风险 | 缓解方案 |
|------|---------|
| PokeAPI 数据量过大 | 首次使用 IndexedDB 缓存，后续脱机运行 |
| 3D 模型性能问题 | 模型异步加载 + LOD；低端设备降级为 2D 精灵图 |
| 技能/特性数量庞大导致平衡性失控 | 先实现"伪随机"版本，后续用数据驱动调优 |
| 种子随机导致 Bug 不可复现 | 所有随机操作通过 SeededRandom，日志输出种子号 |

### B. 预设组合扩展方式

预设组合库设计为数据驱动，便于持续扩充：

```typescript
// 组合库文件结构
src/
├── game/
│   └── data/
│       ├── combos/
│       │   ├── index.ts              # 导出全量组合库
│       │   ├── gen1.ts               # 关都地区热门组合
│       │   ├── gen2.ts               # 城都地区热门组合
│       │   └── meta.ts               # 当前对战环境热门组合
│       └── PresetComboLibrary.ts     # 组合库加载器
```

每只宝可梦的预设组合可选从 Showdown Usage Stats、Smogon Dex 等来源参考编写。

### C. 关键设计决策记录

| 决策 | 选择 | 理由 |
|------|------|------|
| 战斗等级 | 固定 Lv.50 | 消除等级差距影响，聚焦策略博弈 |
| 个体值(IV) | 全 31 | 所有宝可梦一致，排除随机变量 |
| 努力值(EV) | 全 0 | 暂不启用，降低复杂度 |
| 玩家开局选队方式 | 心金魂银对战工厂风格（6 个球，光标选中才展示信息） | 增强盲选策略感与惊喜感 |
| 战斗视角 | 心金魂银第一人称宝可梦视角（我方在画面下方） | 经典沉浸式体验 |
| 敌方组合生成 | 预设组合库（参考对战热门数据）随机抽取 | 保证组合合理性，避免纯随机的逻辑混乱 |
| 敌方特性可见性 | 触发后才揭示，否则 `???` | 保留未知性和首次触发的地雷乐趣 |
| 敌方属性可见性 | 效果绝佳/不好/免疫时逐步揭示 | 鼓励试探进攻，增加战斗的信息获取策略 |
| 重组保留原属性概率 | 30% | 保留一丝"原味"，提供探索乐趣 |
| 存档方式 | 单槽位覆盖 | 肉鸽风格，一次失败全部重来 |
| 属性克制表 | 沿用官方第 6 代+ 版本 | 包含妖精属性，与 PokeAPI 数据一致 |
